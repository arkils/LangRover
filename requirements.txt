langchain>=0.2.0
langchain-openai>=0.1.1
langchain-community>=0.2.0
langchain-ollama>=0.1.0
pydantic>=2.5.3
python-dotenv>=1.0.0
chromadb>=0.5.0

# Serial Communication with ESP32 (required for hardware)
pyserial>=3.5

# Raspberry Pi GPIO (NOT needed - ESP32 handles GPIO)
# RPi.GPIO is no longer used in this architecture
# The ESP32 microcontroller handles all GPIO operations

# Vision
# picamera2 is a system package on Raspberry Pi — do NOT install via pip.
# It is installed automatically by setup.sh via: sudo apt install python3-picamera2
# picamera2
# ultralytics  # YOLO object detection (auto-downloads weights on first run)
# opencv-python  # Computer vision

# Hailo AI HAT+ (optional - for local AI inference on Pi 5)
# hailort
# hailo_model_zoo
